{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "pnpm --dir lib/db run build && pnpm --dir lib/api-zod run build && pnpm --dir artifacts/probate-cms run build && pnpm --dir artifacts/api-server run build"
}
